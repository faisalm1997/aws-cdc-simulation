# Dev configuration

Define local variables for environment config
Set up common tags (Project, ManagedBy, Terraform)
Configure S3 remote state backend
Bucket name pattern: cdc-terraform-state-${environment}
Enable encryption and DynamoDB locking
Generate AWS provider configuration
Point to Terraform source directory