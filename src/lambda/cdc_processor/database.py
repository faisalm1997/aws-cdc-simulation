# Import psycopg2 library

# Define RedshiftConnection class:
#     init method (host, database, user, password, port)
#     connect method (create connection)
#     execute method (run SQL with params)
#     execute_many method (batch operations)
#     close method (close connection)
#     enter and exit for context manager

# Add retry logic for connection failures

# Add logging for connection events